<CottrellCurveBox>:
    orientation:"vertical"
    Label:
        text:"Courbe de Cottrel"
    BoxLayout:
        orientation:"horizontal"
        Label :
            size_hint_x: .80
            text:"Theorique"
        CheckBox:
            
    BoxLayout:
        orientation:"horizontal"
        Label:
            size_hint_x: .80
            text:"Experimentale"
        CheckBox:

<ICurveBox>:
	orientation:"vertical"
	BoxLayout:
		orientation:"horizontal"
		Label:
			size_hint_x: .80
			text:"Courbe I = f(1/sqrt(t))"
		CheckBox:
	BoxLayout:
		orientation:"horizontal"
		Label:
			text:"Selectionner\nun intervalle"
		BoxLayout:
			Label:
				text:"tmin ="
		CentredTextInput:
		BoxLayout:
			Label:
				text:"tmax ="
		CentredTextInput:

<CoxCurveBox>:
	orientation:"vertical"
	Label:
		text:"Courbe Cox"
	BoxLayout:
		orientation:"horizontal"
		Label

<ParametresBox>:
	orientation:"vertical"
	Label:
		text:"Parametres"
	BoxLayout:
		Label:
			text:"Dth"
		BoxLayout:
			orientation:"vertical"
			ButtonPlusMinus:
			Slider:
				orientation:"horizontal"
	BoxLayout:
		Label:
			text:"n"
		ButtonPlusMinus:
	BoxLayout:
		Label:
			text:"s"
		ButtonPlusMinus:
	BoxLayout:
		Label:
			text:"C"
		ButtonPlusMinus:

<CentredTextInput>:
    TextInput:
		input_filter:'float',from_undo: text[:5 - len(self.text)]
		multiline : False
        font_size: .3*self.height
        # left, right
        padding_x:
            [self.center[0] - self._get_text_width(max(self._lines, key=len), self.tab_width, self._label_cached) / 2.0,
            0] if self.text else [self.center[0], 0]
        # top, bottom
        padding_y: [self.height / 2.0 - (self.line_height / 2.0) * len(self._lines), 0]    
    
               
<ButtonPlusMinus>
    orientation:"horizontal"
    Button:
        id: minus
        text:"-"
    CentredTextInput:
        id: zone_de_texte
    Button:
        id:plus
        text:"+"
 

    
<ThreeButton>:
    orientation:"vertical"
    Button:
        on_press:self.root.test1(self)
    ToggleButton:
        group:"troisboutons"
        value:root.premier
        text:"text1"
        state:"down"
    ToggleButton:
        group:"troisboutons"
        value:root.deuxieme
        text:"text2"
    ToggleButton:
        group:"troisboutons"
        value:root.troisieme
        text:"texte 3" 
 
     
       
<Application>
    orientation:"vertical"
    ButtonPlusMinus:
    
    
    
    
    
    
    
    